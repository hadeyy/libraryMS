<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('index') }}">libraryMS</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('index') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-light" href="">smth</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-light" href="">smth</a>
            </li>
        </ul>
        <div class="form-inline my-2 my-lg-0">
            {% if not app.user %}
                <a href="{{ path('login') }}">
                    <button type="button" class="btn btn-outline-info text-light">
                        LOGIN
                    </button>
                </a>

                <a href="{{ path('registration') }}">
                    <button type="button" class="btn btn-outline-info text-light" style="margin-left: 10px;">
                        REGISTER
                    </button>
                </a>
            {% else %}
                <a href="{{ path('logout') }}">
                    <button type="button" class="btn btn-outline-danger text-light">LOGOUT</button>
                </a>
            {% endif %}
        </div>
    </div>
</nav>
