<div id="authorCard" class="card">
    <div class="card-body">
        <div id="authorInfo">
            <h5 class="card-title">
                <span id="authorName">{{ author.firstName }} {{ author.lastName }}</span>
            </h5>
            <h6 class="card-subtitle mb-2">
                <small>from</small>
                <span id="authorCountry">{{ author.country }}</span>
            </h6>
        </div>
    </div>
</div>
