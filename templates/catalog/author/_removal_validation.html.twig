<div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-body">
            <h4>
                <span class="all-caps">
                    {{ author.firstName }} {{ author.lastName }}
                </span>
            </h4>
            <hr>
            <h5>Are you sure you wan to remove this author from the database?</h5>
            <small>*This will also remove all of the author's books. This action cannot be undone.</small>
            <hr>
            <button type="button" class="btn btn-secondary remove-validation-btn" data-dismiss="modal">
                Cancel
            </button>
            <a href="{{ path('delete-author', {id: author.id}) }}">
                <button type="button" class="btn btn-danger remove-validation-btn">REMOVE</button>
            </a>
        </div>
    </div>
</div>
